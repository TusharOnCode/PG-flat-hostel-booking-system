<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Stay Booking Platform</title>

  <!-- FontAwesome Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <!-- AOS Animation CSS -->
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

  <style>
    :root {
      --primary-bg: #e0f7ec;
      --primary-dark: #006d3c;
      --accent: #00b894;
      --accent-dark: #009970;
      --text-light: #555;
      --card-bg: #ffffff;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background-color: var(--primary-bg);
      font-family: 'Poppins', sans-serif;
      color: var(--text-light);
      scroll-behavior: smooth;
    }

    header {
      background-color: var(--primary-dark);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      position: relative;
      box-shadow: 0 4px 10px var(--shadow);
      color: white;
    }
    .hamburger {
      position: absolute;
  display: none;
  flex-direction: column;
  top: 1rem;
      right: 1rem;
  font-size: 1.5rem;
  background: none;
  border: none;
  color: white;
  cursor: pointer;
}



    .logo {
      font-size: 1.8rem;
      font-weight: bold;
      color: white;
    }

    nav {
      display: flex;
      gap: 1.2rem;
      flex-wrap: wrap;
    }

    nav a {
      color: white;
      text-decoration: none;
      font-weight: 500;
      display: flex;
      align-items: center;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: var(--accent);
    }

    nav a .icon {
      margin-right: 0.5rem;
    }

    .hero {
      background: linear-gradient(135deg, #00b894, #006d3c);
      color: white;
      text-align: center;
      padding: 5rem 2rem 4rem;
    }

    .hero h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .hero p {
      font-size: 1.2rem;
      max-width: 700px;
      margin: 0 auto;
    }

    .section {
      padding: 4rem 1rem;
      text-align: center;
    }

    .section h2 {
      color: var(--primary-dark);
      font-size: 2.5rem;
      margin-bottom: 2rem;
    }

    .facilities, .reviews {
      display: flex;
      flex-wrap: wrap;
      gap: 2rem;
      justify-content: center;
    }

    .card {
      background: var(--card-bg);
      padding: 1.5rem;
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      max-width: 300px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }

    .card h3 {
      color: var(--accent-dark);
      margin-bottom: 0.5rem;
    }

    .card p {
      font-size: 1rem;
    }

    #about {
      background-color: white;
      margin: 2rem;
      border-radius: 30px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      padding: 3rem 2rem;
    }

    .sec-content-div {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 2rem;
      max-width: 1100px;
      margin: 0 auto;
    }

    .text-content {
      flex: 1 1 500px;
      font-size: 1.1rem;
      text-align: left;
      line-height: 1.8;
    }

    .image-content img {
      width: 100%;
      max-width: 450px;
      border-radius: 20px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }

    #contact {
      background-color: var(--accent);
      color: white;
      padding: 4rem 2rem;
    }

    .contact-form {
      max-width: 500px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .contact-form input,
    .contact-form textarea {
      background-color: #ffffff;
      color: #333;
      padding: 0.75rem 1rem;
      border-radius: 8px;
      border: none;
    }

    .contact-form button {
      background-color: var(--primary-dark);
      color: white;
      border: none;
      padding: 0.75rem;
      border-radius: 8px;
      font-weight: bold;
      transition: background 0.3s;
    }

    .contact-form button:hover {
      background-color: #004d2a;
    }

    footer {
      background: #006d3c;
      padding: 1rem;
      text-align: center;
      color: white;
    }

    footer a {
      margin: 0 0.5rem;
      color: #ffffff;
    }

    footer a:hover {
      color: var(--accent);
    }

    /* Responsive */
    @media (max-width: 768px) {
       .hamburger {
    display: block;
  }
 header {
        flex-direction: column;
        text-align: center;
      }
      nav {
        display: none; /* Hide nav by default on mobile */
        flex-direction: column;
        background: var(--primary-dark);
        position: absolute;
        top: 60px; /* adjust based on header height */
        right: 2rem;
        border-radius: 6px;
        padding: 1rem;
        width: 150px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        z-index: 100;
      }

 nav.show {
        display: flex; /* Show nav when toggled */
      }

  nav a {
    padding: 0.75rem 1.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }
      .hero h1 {
        font-size: 2.2rem;
      }

      .sec-content-div {
        flex-direction: column;
      }
    }
  </style>
</head>

<body>

 <header>
  <div class="logo">StayBooking</div>
  <button id="hamburger" class="hamburger">
    <i class="fas fa-bars"></i>
  </button>
  <nav  id="navLinks">
    <a href="/"><i class="fas fa-home icon"></i> Home</a>
    <a href="#about"><i class="fas fa-info-circle icon"></i> About</a>
    <a href="/user/register"><i class="fas fa-user-plus icon"></i> Register</a>
    <a href="/user/login"><i class="fas fa-sign-in-alt icon"></i> Login</a>
    <a href="/admin/login"><i class="fas fa-sign-in-alt icon"></i> Admin Login</a>
    <a href="#contact"><i class="fas fa-envelope icon"></i> Contact</a>
  </nav>
</header>

<script>
  function toggleMenu() {
    const nav = document.getElementById('navLinks');
    nav.classList.toggle('show');
  }
</script>

  <section class="hero" data-aos="fade-up">
    <h1>Welcome to Stay Booking Platform</h1>
    <p>Find and book hostels, PGs, and flats easily. Secure your perfect stay now!</p>
  </section>

  <section class="section" data-aos="fade-up">
    <h2>Room Facilities</h2>
    <div class="facilities">
      <div class="card"><h3>📶 Wi-Fi</h3><p>24/7 high-speed internet in every room.</p></div>
      <div class="card"><h3>🛏️ Furnished Rooms</h3><p>Bed, table, chair, and cupboard included.</p></div>
      <div class="card"><h3>❄️ AC Rooms</h3><p>Comfortable air-conditioned rooms available.</p></div>
      <div class="card"><h3>💼 Power Backup</h3><p>Reliable power backup for all rooms.</p></div>
    </div>
  </section>

  <section id="about" data-aos="fade-up">
    <h3 class="section-heading">ABOUT US</h3>
    <div class="sec-content-div">
      <div class="text-content">
        <p>
          Welcome to <strong>Stay Booking</strong> — your trusted partner for finding the perfect place to stay! 
          Whether you need a PG, hostel, or flat, we provide verified listings with easy and secure booking.
          Focused on affordability, safety, and convenience, we help students, working professionals, and families
          find the accommodation they deserve. Join thousands who have found their dream space with Stay Booking.
        </p>
      </div>
      <div class="image-content">
        <img src="/Untitled.png" alt="Stay booking platform showcasing rooms and flats">
      </div>
    </div>
  </section>

  <section id="contact" data-aos="fade-up">
    <h2>Contact Us</h2>
    <form class="contact-form" id="contactform" action="/contact" method="POST">
      <input type="text" name="name" placeholder="Your Name" required>
      <input type="email" name="email" placeholder="Your Email" required>
      <input type="text" name="phone" placeholder="Your phone No." required>
      <textarea name="message" rows="5" placeholder="Your Message" required></textarea>
      <button type="submit">Send Message</button>
    </form>
  </section>


  <footer>
    &copy; <script>document.write(new Date().getFullYear())</script> StayBooking. All rights reserved. |
    Follow us:
    <a href="https://www.instagram.com/lav_raj_0.15/" target="_blank"><i class="fab fa-instagram"></i></a>
    <a href="https://www.linkedin.com/in/lav-raj-7a1a95257/" target="_blank"><i class="fab fa-linkedin"></i></a>
    <a href="https://www.facebook.com/lav.raj.92" target="_blank"><i class="fab fa-facebook fa-lg"></i></a>
    <a href="https://x.com/lav_raj_15" target="_blank"><i class="fab fa-x-twitter"></i></a>
  </footer>

  <!-- AOS Script -->
  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>
    AOS.init();
  </script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  const hamburger = document.getElementById("hamburger");
  const navMenu = document.getElementById("navLinks");

  hamburger.addEventListener("click", (e) => {
    e.stopPropagation(); // Stop event from bubbling
    navMenu.classList.toggle("show");
  });

  document.addEventListener("click", function (e) {
    // Check if clicked element is outside the nav and hamburger
    if (!navMenu.contains(e.target) && !hamburger.contains(e.target)) {
      navMenu.classList.remove("show");
    }
  });
</script>

<script>
  document.getElementById('contactform').addEventListener('submit', async function (e) {
    e.preventDefault();

    const form = e.target;
    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());

    console.log('Form Data:', data);  // Add this line to check the form data

    try {
      const res = await fetch('/', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(data)
      });

      const result = await res.json();

      console.log('Response:', result);  // Log the server response

      if (res.ok && result.success) {
        Swal.fire({
          icon: 'success',
          title: 'Thank you for contacting us!',
          text: result.message
        }).then(() => {
          // Redirect after popup
          window.location.href = '/'; // Optional redirect
        });
      } else {
        Swal.fire({
          icon: 'error',
          title: 'Oops!',
          text: result.message || 'Please check your input.'
        });
      }

    } catch (error) {
      console.error('Error:', error);
      Swal.fire({
        icon: 'error',
        title: 'Server Error',
        text: 'Something went wrong. Please try again.'
      });
    }
  });
</script>

</body>
</html>
